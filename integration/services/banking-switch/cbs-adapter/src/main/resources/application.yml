server:
  port: 8082

spring:
  application:
    name: cbs-adapter
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
    consumer:
      group-id: cbs-adapter-group
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: "*"
        spring.json.type.mapping: "com.bankingswitch.orchestrator.model.CbsRequestEvent:com.bankingswitch.cbsadapter.model.CbsRequestEvent"
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer

kafka:
  topic:
    cbs-request: upi.bank.cbs.request
    cbs-response: upi.bank.cbs.response

cbs:
  host: ${CBS_HOST:http://192.168.29.4:8080}
